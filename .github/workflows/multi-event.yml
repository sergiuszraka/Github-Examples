name: curl ipconfig.org test ip
on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Curl ipconfig.org test"
        run: |
          echo "curl ipconfig.org
         